<%- include('partials/header2') %>

<main class="container mb-5" style="padding-top: 120px;">

    <section id="service-detail-section" class="card shadow-sm mb-5 border-0 d-none" style="background: #f8f9fa;">
        <div class="card-body p-4">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <h5 class="text-uppercase text-muted mb-2 small">Votre sélection</h5>
                    <h1 class="display-6 fw-bold text-primary" id="detail-nom">Chargement...</h1>
                    <p class="lead" id="detail-desc">...</p>
                    
                    <div class="d-flex gap-3 mt-3">
                        <span class="badge bg-dark fs-6 py-2 px-3">
                            ⏱️ <span id="detail-duree">--</span> min
                        </span>
                        <span class="badge bg-secondary fs-6 py-2 px-3">
                            💶 <span id="detail-prix">--</span> €
                        </span>
                    </div>
                </div>
                <div class="col-md-4 text-center d-none d-md-block">
                    <img src="/assets/img/logoblancMASK.png" alt="Icone" style="height: 80px; opacity: 0.5; filter: invert(1);">
                </div>
            </div>
        </div>
    </section>

    <section class="reservation-calendar">
        <h2 class="h3 fw-bold mb-4">📅 Choisir un créneau</h2>

        <div class="card p-4 mb-4 border-0 shadow-sm" style="background: linear-gradient(to bottom, var(--color-primary) 20px, black); color: white;">
            <div class="row g-3">
                <div class="col-md-4">
                    <label class="form-label fw-bold">Salon</label>
                    <select id="business-select" class="form-select"></select>
                </div>
                <div class="col-md-4">
                    <label class="form-label fw-bold">Prestation</label>
                    <select id="service-select" class="form-select"></select>
                </div>
                <div class="col-md-4">
                    <label class="form-label fw-bold">Coiffé(e) par</label>
                    <select id="employee-select" class="form-select">
                        <option value="null">Peu importe</option>
                    </select>
                </div>
            </div>
        </div>
        <br>   
        <div class="calendar-navigation">
            <button id="prev-week" class="btn-secondary calbutton" style="z-index: 10; position: relative;">← Semaine Préc.</button>
            <h4 id="current-period">...</h4>
            <button id="next-week" class="btn-secondary calbutton" style="z-index: 10; position: relative;">Semaine Suiv. →</button>
        </div>

        <div id="calendar-grid" class="calendar-grid bg-white p-3 rounded shadow-sm" style="min-height: 200px;">
            <div class="text-center py-5">
                <div class="spinner-border text-primary" role="status"></div>
                <p class="mt-2">Chargement des disponibilités...</p>
            </div>
        </div>
    </section>

</main>

<%- include('partials/footer2') %>
<script src="/assets/js/reservation.js"></script>